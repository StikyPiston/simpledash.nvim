# simpledash.nvim

**simpledash.nvim** is a Neovim plugin adding a very simple dashboard when you open Neovim without a file to edit.

## Installation (vim.pack)

```lua
vim.pack.add({
    "https://github.com/stikypiston/simpledash.nvim"
})
```
